# Handoff — 2026-09-07, 00:35 МСК

Читать **это** первым, потом `2026-09-06.md` → `2026-09-05.md` → `2026-09-03.md`, потом `CLAUDE.md` + `docs/CODEMAP.md`.

**Git tip:** `f2d35a0` на `origin/main` — `feat: отключение кэша Squid и Domain discover (ADR 0008/0009)`.  
Репо: `MerlKoryAmber/squid-panel`, ветка `main`.

## Приёмка (только человек)

| ADR | Тема | Статус |
|-----|------|--------|
| 0005 | live squid.conf format | **ПРИНЯТО** 2026-09-06 |
| 0006 | AD groups LDAP simple only | в работе / ждёт |
| 0007 | LDAP CA + panel TLS | в работе / ждёт |
| 0008 | Disable Squid object cache | код на git+лабе, **не ПРИНЯТО** |
| 0009 | Domain discover | код на git+лабе, **не ПРИНЯТО** |

Автор кода себе «принято» не ставит.

## Сделано (сессии 06–07.09)

### ADR 0008 — Disable Squid cache
- Settings → Object cache → POST `/settings/cache` → `SquidCachePolicy::setDisabled`.
- БД: `squid_globals.disable_cache`, `cache_dir_saved` (`Database::ensureSchema` + `schema.sql`).
- Builder: при выкл. — `cache deny all` + `cache_mem 0`, skip `cache_dir`; Apply через ADR 0005.
- Файлы: `app/Services/SquidCachePolicy.php`, `SquidConfigBuilder.php`, `SettingsController.php`, `views/settings/index.php`.

### ADR 0009 — Domain discover
- Меню `/discover` → `DiscoverController` → `DomainDiscover` → spmd `domain_discover` (headless Chromium NetLog).
- **Не** Squid / access.log / conf.
- Вывод: только **2nd-level** домены (`toSecondLevel` PHP + `_to_second_level` spmd).
- Галка «Hide common ads/analytics» (default on): **короткий curated denylist** (~78 доменов) → `app/Data/discover_ad_denylist.txt`.
- **Вариант 2 (полный Peter Lowe) — отменён Merl 2026-09-07.** Не возвращать без команды.
- SSRF fail-closed; lock `/run/spmd/discover.lock`; нужен Chromium на хосте.
- Тест: `tests/domain_discover_cli.php` (в т.ч. `ok short denylist`, `ok not full Peter Lowe seed`).

### Документы / процесс
- `docs/CODEMAP.md` — карта (обновлять **перед каждым push**).
- `CLAUDE.md` / `.cursor/rules`: handoff → CODEMAP; caveman; lab-first.
- Deferred: 0008/0009 сняты с backlog; остались DevTools-сниппет и Cascade Edit.

## Лаба (факт на 07.09 ~00:25 МСК)

- Хост: `root@192.168.0.178`, UI `https://192.168.0.178:8443/`, код `/opt/spm`.
- 0008/0009 + короткий denylist **выложены** (tar/scp), `php tests/domain_discover_cli.php` — ok, `/` и `/discover` → **302**.
- Chromium ставили (`dnf install chromium --exclude=openh264`); после правок `spmd.py` — `systemctl restart spmd`.
- SSH с Windows часто флакает (`GetConsoleMode`, reset). После успеха выкладки процесс можно убить, если hung — смотреть stdout до kill.
- **Не** запускать `update.sh` без команды (сносит `spm.db`, переписывает live conf).
- **Не** `systemctl restart squid` без команды.

### Грабля (уже чинили)
Кратко положили `DiscoverController.php` в `app/Services/` → HTTP 500 (двойной класс). Удалить stray; Controllers только в `app/Controllers/`. Перед «готово» — curl UI на лабе.

## Горячие пути

| Что | Где |
|-----|-----|
| Discover | `DiscoverController`, `DomainDiscover`, `views/discover/`, denylist `app/Data/discover_ad_denylist.txt` |
| Cache off | `SquidCachePolicy`, Settings `/settings/cache` |
| spmd | `agent/spmd.py` → `domain_discover`; whitelist = `PrivilegedExecutor` |
| Apply live | ADR 0005: parse → backup `*.spm-policy-*` → write → reconfigure |
| Карта | `docs/CODEMAP.md` |
| Отложенное | `docs/agent_reports/deferred/README.md` |

## Открыто / не делать без команды Merl

1. Приёмка 0006–0009.
2. Instructions / DevTools-сниппет.
3. Cascade Edit (сейчас только Delete+Add).
4. Возврат полного Peter Lowe denylist.
5. Push / commit / `update.sh` / restart Squid — только по явной команде (коммит+пуш в одном сообщении Merl — ок, делать сразу).

## Выкладка (порядок)

1. `scp`/tar → `/opt/spm` (после `spmd.py` → `systemctl restart spmd`).
2. Человек смотрит на лабе.
3. Предложить / сделать commit (явный список файлов; **никогда** `git add .`).
4. Push — отдельным шагом, если не сказал «коммит пуш» сразу.
5. Перед push — сверить `docs/CODEMAP.md`.

## Стиль

- Ответы: caveman (`CLAUDE.md` §13), пока Merl не снимет.
- Вопросы человеку — форма с вариантами, рекомендуемый первым.
- Время в доках/ответах — **МСК** (UTC+3).
- Коммиты: Conventional Commits, текст после `:` по-русски.
- Author в git на этой машине часто не в config → одноразово env `GIT_AUTHOR_*` / `GIT_COMMITTER_*` как в истории: `Merl <312885698+MerlKoryAmber@users.noreply.github.com>`. **Не** `git config`.
