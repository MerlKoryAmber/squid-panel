# Handoff — 2026-08-23, 14:56 МСК

Читать **это**, потом `CLAUDE.md`, `docs/adr/README.md`.

**Git:** `main` = `origin/main`, HEAD **`6501891`**.  
Репо: https://github.com/MerlKoryAmber/squid-panel  
Владелец: Merl. Push только по команде; `git add .` / force-push нельзя.

## Сейчас (не ПРИНЯТО человеком)

1. ~~**Instructions + Kerberos children + Dashboard Negotiate**~~ — **ПРИНЯТО** Merl, 2026-08-23.
2. **ADR 0005** — Save политики → полный live `squid.conf` после parse. Код на git; **прод** Merl проверяет отдельно (handoff 21.08).
3. **Каскад:** правка правила — пока Delete+Add (Merl, 23.08). Edit не делали.
4. ADR 0001, 0002 — в работе, без задачи не трогать.

**Не SPM:** централизованный файрвол (агент Win/Linux) — **другой проект**, в бэклог SPM не класть.

## Приёмка на лабе (Merl)

**ПРИНЯТО** 2026-08-23 — Instructions, Kerberos children/startup/idle, Dashboard Negotiate, шапка.

~~Чеклист~~ (архив):

| # | Где | Что смотреть |
|---|-----|----------------|
| 1 | Sidebar внизу | **Instructions** (EN), одна кнопка |
| 2 | Instructions | `?g=keytab` / `?g=keytab-merge` |
| 3 | Kerberos | Children / Startup / Idle |
| 4 | Dashboard | Negotiate helpers |
| 5 | Шапка | без ступеньки |

## Недавно в git (после fdc4972)

- `bc4f6e7` — Instructions (keytab guides), sidebar
- `ee940f1` — шапка/бренд 64px
- `6501891` — Kerberos startup/idle; `NegotiateHelperStats` на Dashboard

## Выкладка UI

`scp` → `/opt/spm` → Merl смотрит → commit → push отдельно.  
`update.sh` только по команде (**сносит `spm.db`**). Live conf — parse+apply, не руками.

## Хвост (не блокирует приёмку UI)

- Kerberos **Service Principal** vs `GSS_C_NO_NAME`: старый commented шаблон Squid ≠ live; Save с пустым principal не пишет `-s`.
- Handoff 2026-08-21 — исторический; Apply на прод — см. ADR 0005 / policy-apply REPORT.
- Отчёт по этой пачке: `docs/agent_reports/ui-instructions-kerberos/REPORT.md`.
