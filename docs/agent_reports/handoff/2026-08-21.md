# Handoff — 2026-08-21, 01:15 МСК

Читать **это**, потом `CLAUDE.md`, `docs/adr/README.md`. Чат не восстанавливать.

**Git:** `main` = `origin/main`. Код Apply+hotfix ≥ **`49a8bc3`**. Этот handoff — коммит `docs: handoff 2026-08-21`. `git log -1`.  
Репо: https://github.com/MerlKoryAmber/squid-panel  
Владелец: Merl. Коммит: `git -c user.name="Merl" -c user.email="312885698+MerlKoryAmber@users.noreply.github.com"` (не выдумывать GitHub id). Push только по команде; `git add .` / force-push нельзя.

## Сейчас (не ПРИНЯТО человеком)

1. **ADR 0004 Apply to Squid** — код на git, **на проде parse падал**, hotfix `49a8bc3` ещё **не проверен** после выкладки.
2. Simple UI (ADR 0003 ПРИНЯТО как идея) — ящик справа, 5 колонок, превью членов, без бейджа Complex. **UI не принят** глазами после последнего update.
3. ADR 0001, 0002 — в работе, не трогать без задачи.

## Завтра первое

Выкладка `49a8bc3`+ : `/opt/update.sh` (**сносит `spm.db`**, импорт live conf) → **`systemctl restart spmd`** (иначе старый Python).  
Dev-логин панели: **admin / admin**.  
Save правил ≠ Squid. **Apply to Squid** (Settings или Access).

Проверка Apply: cache.log без FATAL; в live conf `include /etc/squid/spm-acl.conf` **после** `external_acl_type` / `auth_param`; backup `/etc/squid/squid.conf.spm-policy-*`; `acl DIT_AD external www_DIT_Allow` не до helper. Parse fail → live не менять.

Успех → человек пишет **ПРИНЯТО** (Apply и/или Simple). Агент сам не принимает.

## Баг, уже в git

`5ad2985` вставлял include у **первого** `acl`. Live: `auth_param` → include → `acl DIT_AD external www_DIT_Allow` → helper ещё нет → FATAL `Bungled spm-acl.conf`. Staging `/opt/spm/storage/tmp/squid.conf.parse`.  
`49a8bc3`: include после последнего `auth_param` / `external_acl_type` / `spm-listen`. Тест-зеркало: `tests/policy_include_place.php`.

## Не делать

- Целиком переписывать `/etc/squid/squid.conf` (`SquidConfigBuilder::save()` бросает).
- Apply на каждый Save; auth/ssl_bump/refresh_pattern из БД в v1.
- Рестарт Squid / push без команды.
- Обходить CONNECT/порты в Simple; слово ACL в Simple Access.

## Карта кода Apply

- Фрагменты: `app/Services/SquidConfigBuilder.php` + `SquidPolicyApply.php`
- Агент: `agent/spmd.py` `apply_squid_policy` / `_ensure_policy_includes`
- UI: `POST /settings/apply-policy` — Settings + Simple Access
- Listen (уже было): `spm-listen.conf`, не путать с policy

## Simple (факты)

Save → `http_access_rules` в `spm.db`. Клик строки → ящик Edit (не просмотр KWTS). Сложные: те же колонки, клик → Expert. Члены списков не править в правиле (Lists). Каскад без ящика.

## Выкладка

`update.sh` = полная переустановка панели, **db drop**. После смены `spmd.py` обязателен restart агента. nginx :8443, PHP-FPM `squidmgr`. PHP на Windows-агенте часто нет — тесты на сервере: `php /opt/spm/tests/squid_fragments_cli.php` и `policy_include_place.php` / `policy_ui_cli.php`.
