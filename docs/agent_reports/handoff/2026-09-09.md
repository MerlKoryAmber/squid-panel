# Handoff — 2026-09-09, 18:40 МСК

Читать **это** первым, потом `2026-09-08.md` → evening `2026-09-07` → …, потом `CLAUDE.md` + `docs/CODEMAP.md`.

**Git tip:** `c51f9ee` на `origin/main` (`fix: update/spm не падают из снесённого cwd`).  
Репо: `MerlKoryAmber/squid-panel`, ветка `main`.

## Выкладка (не менялось)

| Где | Как |
|-----|-----|
| **Тест / прод** | Только `update.sh --keep-db` / **`spm` → 1** / `spm update`. **Не** раскладывать файлы руками в `/opt/spm`. |
| Первый update после снесённого cwd | Один раз: `cd /tmp` → затем update (старый `/opt/update.sh` иначе падает на `getcwd`). После `c51f9ee` CLI сам уходит в `/tmp`. |
| CLI | `/usr/bin/spm` (меню). Паттерн для других репо: `docs/patterns/cli-menu-linux.md` |
| Live `squid.conf` | Только parse+Apply панели. Squid restart — только по команде Merl. |
| Author commit (Windows) | env `GIT_AUTHOR_*` = Merl, **не** `git config`. «Коммит пуш» — сразу. |

## Сегодня (09.09) — что сделано и на git

### XFF per peer → на проводе PASS
- Emit: `request_header_access X-Forwarded-For deny all` + `request_header_add X-Forwarded-For "%>a" <ACL>` из **allow** `cache_peer_access` пира с галкой (не `peername` — на проде не матчился).
- Коммиты: `9dfeac6` → `a727695` → **`7a9ecd8`**.
- tcpdump к MB: `X-Forwarded-For: 172.22.10.25` на `vk.com` — **OK**.
- После XFF клиент видит ответы **MB** (302/301 kittenx, dnf `SSL wrong version number` = HTTP в TLS-туннеле). Чинится политикой MB по client IP, не Squid SSL.

### Timezone панели
- Settings → Timezone: `Europe/Moscow` \| `UTC` (`PanelTimezone`, `settings.timezone`).
- Коммит: `a945057` (вместе с cwd-restore для update).

### CLI меню `spm`
- `spm.sh` → `/usr/bin/spm` (+ local). Пункты: update keep/drop, uninstall, password, status, restart spmd/web, backup, URL. **Нет** restart Squid.
- PATH: только `/usr/local/bin` ломал root → `/usr/bin` (`17773fc`).
- Update из снесённого `/opt/squid-panel` → `getcwd` + `set -e` (`c51f9ee`).
- Паттерн (без чужих брендов): https://github.com/MerlKoryAmber/squid-panel/blob/main/docs/patterns/cli-menu-linux.md

### Chromium / Discover (раньше сегодня / вчера в tip)
- `650951d`: chromium не upgrade на каждый update; Discover без autoplay/media.

## Горячие пути

| Тема | Где |
|------|-----|
| XFF emit | `SquidConfigBuilder` (`xffRequestHeaderAddLines`) |
| Timezone | `PanelTimezone`, Settings General |
| CLI | `spm.sh`, `install.sh`, `update.sh` |
| never_direct order | `orderedRoutingRules` (ещё с 08.09) |
| Карта | `docs/CODEMAP.md` |
| Паттерн меню | `docs/patterns/cli-menu-linux.md` |

## Открыто / не делать без команды

1. Приёмка ADR 0006–0010, XFF/timezone/CLI — **не ПРИНЯТО** автором.  
2. MB: trust/политика для `172.22.10.25` (dnf/HTTPS после XFF) — на стороне MB.  
3. Cascade Edit (Delete+Add).  
4. Instructions / DevTools-сниппет.  
5. Шифрование LDAP bind_password в SQLite.  
6. Пункт Squid restart в `spm` — только если Merl явно попросит.

## Стиль

- Caveman, пока Merl не снимет.  
- Вопросы — форма с вариантами.  
- Время — **МСК**.  
- На сервере ничего руками (scp файлов / `install -m` в обход update) — только штатный update/`spm`.
