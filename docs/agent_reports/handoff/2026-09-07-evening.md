# Handoff — 2026-09-07, 18:50 МСК

**Актуальный handoff:** `2026-09-08.md` (читать первым).

Читать **это** как контекст дня 07.09 (ADR 0010 / тест), потом `2026-09-07.md` → …, потом `CLAUDE.md`.

**Git tip (на момент вечера 07.09):** см. историю; актуальный tip — в `2026-09-08.md`.  
Репо: `MerlKoryAmber/squid-panel`, ветка `main`.

## Контекст дня (тест, не лаба)

- Merl: **сегодня только тест** (не лаба `192.168.0.178`, пока не скажет иначе).
- Выкладка: `sudo bash /opt/update.sh --keep-db` (не `update.sh` без флага — спросит; без keep сносит db).
- После кода с `spmd.py` / timer: `systemctl restart spmd`; `systemctl enable --now spm-ad-group-sync.timer`.
- **Не** `systemctl restart squid` без команды. Apply/reconfigure — через панель / format_live.

## Приёмка (только человек)

| ADR | Тема | Статус |
|-----|------|--------|
| 0005 | live squid.conf format | **ПРИНЯТО** 2026-09-06 |
| 0006 | AD LDAP simple bind | частично superseded 0010 для Squid; sync всё ещё simple bind |
| 0007 | LDAP CA + panel TLS | в работе / ждёт |
| 0008 | Disable Squid cache | код на git, **не ПРИНЯТО** |
| 0009 | Domain discover | код на git, **не ПРИНЯТО** |
| **0010** | AD members → DB → `proxy_auth` file | код на git+тест, **не ПРИНЯТО** |

Автор кода себе «принято» не ставит.

## ADR 0010 — сделано (сессия 07.09 день)

Решение: **нет пароля в squid.conf**. Членство = Sync LDAP → `ad_group_members` → `/etc/squid/acl.d/ad_*.txt` → `acl ad_* proxy_auth -i "…"`.

| Коммит | Суть |
|--------|------|
| `e055b6e` | feat ADR 0010: sync, timer, migrate kg_*, builder без helper/-p |
| `0ce73fa` | fix: `format_live.php` migrate до parse (иначе keep-db: helper выкинут, `acl external kg_*` → parse fail) |
| `d819350` | fix: `proxy_auth -i` — Kerberos LOGIN часто `User@REALM`, файл lowercase |

### Горячие файлы
- `app/Services/AdGroupMemberSync.php`, `AdGroupAcl.php`, `AdLdapConfig.php`
- `app/Services/SquidConfigBuilder.php` (`proxy_auth -i` для file)
- `app/Services/SquidPolicyApply.php` (migrate до generate)
- `install/format_live.php` (migrate + пустые work-файлы + post-check)
- `agent/spmd.py` → `ad_ldap_group_members`; timer `spm-ad-group-sync.{service,timer}`
- `install/ad_group_sync.php`
- ADR: `docs/adr/0010-ad-group-member-sync.md`
- Отчёт: `docs/agent_reports/ad-group-sync/REPORT.md`

### Операционка после update на тесте
1. `restart spmd` + `enable --now spm-ad-group-sync.timer`
2. UI AD groups → **Sync members** (до Sync файлы могут быть пустые → fail-closed, никто не match)
3. Apply политики / Cascade Save (чтобы в conf был `proxy_auth -i`)
4. Проверка: `grep 'proxy_auth -i' /etc/squid/squid.conf`; `head` у `acl.d/ad_*.txt`; логин в access.log vs файл

### Грабли (уже пойманы)
1. **format_live без migrate** + keep-db → Squid parse fail на orphan `external kg_*`. Починено `0ce73fa`.
2. **Регистр:** файл lowercase, access.log с большой буквы → без `-i` не match. Починено `d819350`.
3. Каскад: from+to = **AND**. Если кроме группы выбран ACL сайтов — оба должны совпасть.
4. Import создаёт пустой file ACL — без Sync правило молчит.

### Не принято / хвост 0010
- Приёмка Merl на тесте (каскад после `-i` — проверить повторно после update+Apply).
- Шифрование `bind_password` в SQLite — отдельно.
- ADR 0001 формулировка про dump в proxy_auth — superseded 0010 (не откатывать без команды).

## Утро 07.09 (кратко, детали в `2026-09-07.md`)

- ADR 0008 cache off, ADR 0009 discover + короткий denylist, Chromium в install, NetLog без google-шума.
- Лаба была целью утром; **день — тест**.

## Открыто / не делать без команды Merl

1. Приёмка 0006–0010.
2. Instructions / DevTools-сниппет.
3. Cascade Edit (сейчас Delete+Add).
4. Полный Peter Lowe denylist — **отменён**, не возвращать.
5. Push / commit / `update.sh` / restart Squid — только по явной команде («коммит пуш» в одном сообщении — делать сразу).

## Выкладка

1. Тест сегодня: `update.sh --keep-db` предпочтительнее ad-hoc scp.
2. Лаба: tar/scp → `/opt/spm` только если Merl скажет; после `spmd.py` → `restart spmd`.
3. Commit — явный список файлов; **никогда** `git add .`.
4. Перед push — CODEMAP актуальный.

## Стиль

- Caveman (`CLAUDE.md` §13), пока Merl не снимет.
- Вопросы — форма с вариантами, рекомендуемый первым.
- Время — **МСК**.
- Коммиты: `type: русский текст`.
- Git author env при необходимости: `Merl <312885698+MerlKoryAmber@users.noreply.github.com>`. **Не** `git config`.
