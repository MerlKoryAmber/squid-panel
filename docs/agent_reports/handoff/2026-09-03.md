# Handoff — 2026-09-03, 19:30 МСК

Читать **это**, потом корневой `CLAUDE.md`, `docs/adr/README.md`.

Предыдущий дневной handoff (UI Instructions/Kerberos): `docs/agent_reports/handoff/2026-08-23.md` — не отменяет это.

**Git:** ветка `main` = `origin/main`, HEAD **`7162068`**.  
Репо: https://github.com/MerlKoryAmber/squid-panel  
Владелец: **Merl**. `git add .` / force-push / `systemctl restart squid` без команды — нельзя.

**Автор коммитов на Windows без git config:** env `GIT_AUTHOR_NAME=Merl`, `GIT_AUTHOR_EMAIL=312885698+MerlKoryAmber@users.noreply.github.com` (и COMMITTER те же). `git config` не трогать.

**Merl 2026-09-03 ~17:55 МСК:** «коммит» / «пуш» по запросу — делать сразу, без меню «делаем или нет». Исключения: секреты в diff, force-push, `update.sh` без `--keep-db`.

## Стек / лаба

- Панель PHP + `spmd` (Python), SQLite `spm.db`, Squid, nginx **:8443**, PHP-FPM `squidmgr`.
- Лаба: `root@192.168.0.178` (SSH `~/.ssh/config`, `IdentityFile ~/.ssh/id_ed25519`). Хост `lab`.
- UI: `https://192.168.0.178:8443/`
- Выкладка: `scp` в `/opt/spm` (не `update.sh` без команды). `update.sh` клонит GitHub и **может снести `spm.db`**; с сохранением БД: `sudo bash /opt/update.sh --keep-db` — только если Merl сказал и код уже на GitHub.
- После правок `agent/spmd.py`: **`systemctl restart spmd`**.
- Локально на Windows агента PHP часто нет — синтаксис/`php tests/*.php` гонять на лабе.

Не коммитить: `deploy-tls.tar` (локальный архив выкладки, untracked).

## Зачем всё это (прод, ~2 дня после выката)

Часть пользователей — prompt вместо transparent Negotiate.

Логи (смысл, не чинить вслепую):

- LDAP group helper: `Can't contact LDAP server`, SASL GSSAPI bind, timeout.
- SRV `_ldap._tcp` тащил и `hdc-*`, и `bhdc-*` (филиальные DC).
- Также Negotiate `BH … type 1 NTLM token`, helper spawn `Starting 20/40`.
- Dashboard виджет Negotiate смотрит «All N/M busy» / FATAL queue — **зелёный ≠ здоровый**.

Решение групп: **только LDAP simple bind** на pin’нутые DC (`hdc-*`), не GSSAPI. Negotiate Kerberos — только SSO пользователя.

## HEAD и что в коде

| Коммит | Что |
|--------|-----|
| **`7162068`** | ADR **0007**: CA в system trust + TLS панели через spmd; UI Choose file как Kerberos |
| `9a14fe8` | ADR **0006**: AD groups **только** LDAP simple; GSSAPI для групп убран |
| `e431cce` | LDAP simple bind primary (ещё был dual-mode, потом вырезан) |
| `e9770ba` | pin DC `-S` для group helper |
| `f503bde` | install/update: пароль admin не сбрасывать при `--keep-db`; nginx :80 busy + IPv6 listen strip |
| `0b33855` / `567ff4d` | Access Logs: newest-first, sort, peer filter; hang fix 16 MiB tail |

ADR: `docs/adr/0006-ad-ldap-simple-bind.md`, `docs/adr/0007-ldap-ca-panel-tls.md` (статус **в работе**, не ПРИНЯТО).

Отчёты: `docs/agent_reports/ad-ldap-simple/REPORT.md`, `docs/agent_reports/tls-ca-panel/REPORT.md`.

## AD groups (0006)

- UI: `/acl/ad-groups` — servers, port, SSL, bind DN, password, base DN. Import → ACL `ad_*` + helper `kg_*`.
- Таблица `ad_ldap_config`. Helper: `-u/-p/-b/-l`/`-S`, **не** GSSAPI/keytab для групп.
- Список групп: spmd staging JSON + `ldapsearch -x -y`.
- Ключи: `app/Services/AdLdapConfig.php`, `AdGroupAcl.php`, `AdGroupController.php`, `views/acl/ad_groups.php`, `agent/spmd.py`.

## CA + TLS панели (0007) — лаба уже была scp + restart spmd ~15:33 МСК

**Root CA (LDAPS):** POST `/acl/ad-groups/ca`  
spmd `ca_trust_install`:

- `/etc/pki/ca-trust/source/anchors/spm-ldap-ca.crt` + `update-ca-trust extract`
- копия `/etc/squid/spm-ldap-ca.pem`

С CA: helper без `-a`; `ldapsearch` `LDAPTLS_REQCERT=demand` + `LDAPTLS_CACERT`. Без CA: fallback `-a` / `REQCERT=never`.

**Panel TLS:** POST `/settings/tls`  
spmd `panel_tls_install` → те же пути nginx что install.sh:

- `/etc/pki/tls/certs/spm-selfsigned.crt`
- `/etc/pki/tls/private/spm-selfsigned.key`

`nginx -t` + reload; при ошибке откат.prev файлов.

Код: `app/Services/PanelTls.php`, whitelist в `PrivilegedExecutor.php` + `agent/spmd.py`. Тест: `tests/panel_tls_cli.php` (на лабе).

Upload UI: hidden file + `btn btn-secondary` **Choose file** + имя файла (как `/auth/kerberos`).

Роуты в `public/index.php`.

## Статус приёмки

**РЕАЛИЗОВАНО НО НЕ ПРИНЯТО** (Merl не ставил ПРИНЯТО):

- LDAP-only AD groups (0006)
- CA + panel TLS (0007)

Независимая верификация другим агентом по §4 CLAUDE.md на 0007 **не делалась**. Автор себе «принято» не ставит.

## Хвост / завтра

1. **Прод:** сначала загрузить **Root CA**, потом LDAPS Yes. Без CA LDAPS скорее всего сломается или останется verify-off.
2. Лаба: живой upload CA и panel cert глазами (если Merl ещё не прогнал).
3. После `update.sh --keep-db` на любой машине — снова **`systemctl restart spmd`**.
4. Старый хвост (08-23): правка правила каскада всё ещё Delete+Add; ADR 0001/0002 без задачи не трогать.
5. Dashboard Negotiate ≠ полная диагностика auth.

## Не делать без команды Merl

- push уже сделан для `7162068`; новый push — только если он попросил.
- `update.sh` без `--keep-db`.
- `systemctl restart squid`.
- Force-push, `git add .`.
- Коммит `deploy-tls.tar`.

## Чат этой сессии (Cursor)

Transcript: [TLS CA panel handoff](d3545c7c-7e59-4ae0-8268-9a2fe409a3ce) — LDAP groups + CA/TLS + лаба + commit/push `7162068`.
